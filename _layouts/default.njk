<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Every Vote Counts - UVM</title>
        <link rel="apple-touch-icon" sizes="180x180" href="assets/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="assets/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="assets/favicon-16x16.png">
        <link rel="manifest" href="assets/site.webmanifest">
        <style>

            body {
                margin: 0;
                font-size: 1.6em;
                line-height: 2em;
            }

            header {
                display: flex;
                flex-direction: column;
            }

            .evc-banner {
                background-color: #B2D23E;
                color: #007155;
                text-align: center;
                padding: 10px 0;
                font-size: 1.6em;
                margin: 15px 0;
                transition: ease 0.2s all;
                line-height: 1.1em;
            }

            .uvm-banner {
                color: white;
                background-color: #F47D20;
                padding: 0 25px;
                display: flex;
                align-items: center;
                float: right;
                margin-bottom: 15px;
                font-size: 0.5em;
            }

            .uvm-banner .uvm-logo {
                height: 50px;
                margin-right: 10px;
            }

            .blue-block {
                background-color: #BBE5FB;
                position: relative;
                overflow: hidden;
                z-index: -1;
            }

            .blue-block .inner-section:after {
                content: '';
                position: absolute;
                left: 0;
                top: 0;
                width: 100%;
                height: 100%;
                z-index: -1;
                opacity: 0.1;
                background-image: url("assets/topography.png");
                -ms-background-size: cover;
                -o-background-size: cover;
                -moz-background-size: cover;
                -webkit-background-size: cover;
                background-size: cover;
            }

            section {
                margin-bottom: 40px;
                display: flex;
                flex-direction: column;
            }

            .inner-section {
                margin: 0 10px;
            }

            .light-theme h2 {
                color: #007155;
            }

            .dark-theme {
                background-color: #007155;
                color: white;
            }
            .dark-theme h2 {
                color: #D9F420;
            }

            a {
                text-decoration: none;
                color: #3B84DD;
                font-weight: bold;
            }

            .voting-rate-pic {
                max-height: 100%;
                max-width: 100%;
                object-fit: scale-down;
            }

            .contact-info {
                display: grid;
                grid-template-areas: "profile email" "profile phone";
                grid-template-columns: repeat(2, minmax(auto, 500px));
                justify-content: center;
            }

            .contact-info * {
                display: flex;
                justify-self: center;
                align-self: center;
            }

            .profile-pic {
                height: 150px;
                border-radius: 50%;
                grid-area: profile;
            }

            .email {
                grid-area: email;
            }

            .phone {
                grid-area: phone;
            }

            .phone:before {
                content: "\260e";
                margin-right: 0.5em;
            }

            /* Utilities */
            .bold {
                font-weight: bolder;
            }

            @media only screen and (min-width: 780px) {
                .inner-section {
                    margin: 0 25%;
                }

                .evc-banner {
                    margin: 0 0 15px;
                    padding: 15px;
                }

                header {
                    font-size: 1.5em;
                }
            }
        </style>

    </head>

    <!-- If you're looking at the code, chances are, you might want to contribute to this project. Check out the Github repo here: https://github.com/JamesTedesco/uvm.vote -->
    <body>

        <header>

            <div class="blue-block">
                {# <img src="assets/topography.png" class="topography" alt="topographical style overlay"> #}
                <h1 class="evc-banner">Every Vote Counts</h1>
                <div class="uvm-banner">
                    <img src="assets/uvm-logo.png" class="uvm-logo" alt="UVM Logo">
                    <h1>UVM</h1>
                </div>
                <div class="inner-section">
                    <p>We're the University of Vermont's chapter of Every Vote Counts National. EVC is a student lead organization focused on civic engagement, with tenet focuses on voter registration, education, and reform.</p>
                </div>

            </div>
        </header>

        <main>

            {{ content | safe }}
        </main>

    </body>
</html>